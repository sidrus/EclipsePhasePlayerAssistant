<div class="navbar navbar-inverse navbar-fixed-top">
  <div class='navbar-inner'>
    <div class="container-fluid">
      <%= link_to 'Eclipse Phase', root_url, class: 'brand' %>
      <div>

        <ul class="nav">
          <li class="divider-vertical"></li>
          <li class=""><a href="<%= root_url %>">Home</a></li>

          <li class="divider-vertical"></li>
          <% if user_signed_in? then %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">My Characters <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'Create New...', new_character_url %></li>
                  <li><%= link_to 'View My Characters', characters_url %></li>
                </ul>
              </li>
              <li class="divider-vertical"></li>

              <li class="pull-right"><%= link_to 'Logout', destroy_user_session_url, method: :delete %></li>
          <% else %>
              <li><%= link_to 'Login', new_user_session_url %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
